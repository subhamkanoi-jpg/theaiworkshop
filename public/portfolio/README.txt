Put website screenshots here, then point to them in src/App.tsx (the `portfolio` array near the top).

Recommended: 16:10 ratio, ~1200x750px, .png or .jpg, optimized/compressed.

Example file names already referenced in code:
  ur-hospitality.png
  shreejee-infotech.png
  aiworkshop.png
  four.png
  five.png

Until a real image exists at the referenced path, the card shows a gradient placeholder automatically.
