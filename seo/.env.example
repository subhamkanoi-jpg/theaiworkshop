# v-serpbear (http://localhost:3030)
PASSWORD=change-me
SECRET=change-me-to-a-long-random-string
APIKEY=change-me-serpbear-api-key
NEXT_PUBLIC_APP_URL=http://localhost:3030

# Elmo lives in the directory `elmo init` created (often ./elmo or ~/.elmo).
# Put ADMIN_API_KEYS and a provider key there, not here.
# Then: ELMO_URL=http://localhost:1515 ELMO_API_KEY=... node seo/seed-elmo.mjs
